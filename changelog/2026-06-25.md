# 2026-06-25 改动总结：站立任务改造为速度跟踪行走 + sim-to-real 噪声鲁棒化

## 背景

真机测试发现两个问题：
1. **站立时两腿快速交替抖动** —— 根因是训练时观测**完全没有噪声、没有动作延迟**，策略学成了对干净观测高度敏感的高增益控制器，上真机遇到 IMU/电机噪声和通信延迟就抖。用户采的 30s 静置 IMU 数据（`imu_noise_static_2026-06-25.csv`）显示噪声其实很小（gyro std ~0.003 rad/s），证实静置噪声不是主因，真正缺的是 domain randomization。
2. **频繁小碎步、迈不开** —— 根因是当前任务**根本不是行走**：`_commands` 恒为 0，`lin_vel` 奖励实际在惩罚一切机体移动，这是个"零指令站立 + 抗推力"任务。reward 里也没有任何步态/接触项，策略对"怎么迈步"无偏好，收敛到原地碎步这个局部最优。

用户确认最终目标是**速度跟踪行走**，命令维度为**仅 vx 前进**，并**保留原地站立能力**（部分 env 采样零速度指令）。

涉及文件：
- `source/oceanisaaclab/oceanisaaclab/tasks/direct/oceanisaaclab/oceanisaaclab_env_cfg.py`
- `source/oceanisaaclab/oceanisaaclab/tasks/direct/oceanisaaclab/oceanisaaclab_env.py`

## 改动 1：sim-to-real 噪声鲁棒化（解决抖动）

### 观测高斯噪声
cfg 新增 `enable_obs_noise` + 4 个 std；`_get_observations` 在原始物理量上加噪后再乘 scale。

| 参数 | 值 | 说明 |
|------|-----|------|
| `noise_ang_vel` | 0.03 rad/s | 静置实测 ~0.003，留余量覆盖运动振动 |
| `noise_proj_g` | 0.02 | 静置实测 ~0.0004，覆盖姿态估计误差 |
| `noise_joint_pos` | 0.01 rad | 编码器噪声 |
| `noise_joint_vel` | 1.0 rad/s | **差分速度噪声大，常为腿抖主因** |

### 动作延迟随机化
cfg 新增 `enable_action_latency` / `action_latency_steps=2`。`__init__` 加 action 环形缓冲 + 每 env 随机延迟；`_pre_physics_step` 把延迟后的动作应用到物理端（观测仍喂策略自己刚下发的 `_actions`，符合真机语义）；`_reset_idx` 重置缓冲并重采样延迟步数。

## 改动 2：行走任务改造（解决碎步）

**关键：观测维度不变（仍 39）**，`commands(3)` 早已在观测里，只是从没非零采样过，无需改网络结构。

### 命令采样（`_reset_idx`）
- 按 `command_vx_range=(-0.3, 0.8)` m/s 采样 vx（前进为主，允许少量后退）；vy、yaw 保持 0。
- `stand_still_prob=0.2`：该比例 env 置零速度指令，保留站立能力；用 `_is_standing` 布尔 mask 记录，门控 air-time 奖励。

### 接触传感器（cfg + `_setup_scene`）
- cfg 新增 `ContactSensorCfg`，监测双脚 `leg_[lr]5_link`，`track_air_time=True`。
- **踩坑修复**：Ocean URDF 把腿 link 导入成**嵌套**结构，IsaacLab 的 `activate_contact_sensors` BFS 到 `base_link` 就停，脚端拿不到接触 API。在 `_setup_scene` 里 clone 之前手动给脚端 prim 补 `PhysxContactReportAPI`（`_activate_feet_contact_sensors`）。
- 接触传感器索引（`_feet_contact_ids`）与 articulation 索引（`_feet_body_ids`）是两套独立索引，均用 `preserve_order=True` 按 [r5, l5] 顺序解析对齐。

### 奖励重构（`compute_rewards`）
| 奖励项 | 改动 | 说明 |
|--------|------|------|
| `track_lin_vel` | **新增** | exp 跟踪 vx 指令，正号，行走核心驱动 |
| `lateral` | **新增** | 惩罚非指令的 vy 侧移和 yaw 自转 |
| `ang_vel` | **改** | 从罚全三轴改为只罚 roll/pitch（避免与转向冲突） |
| `feet_air_time` | **新增** | 落地瞬间奖励腾空时长，仅非站立 env，碎步克星 |
| `feet_slide` | **新增** | 接地时惩罚水平滑移 |
| `stand_still` | **新增** | 站立 env 惩罚脚移动 |
| `action_rate` | **改** | 惩罚从 -0.01 加大到 -0.02 抑制高频抖动 |
| 保留 | — | alive / terminated / upright / height / joint_pos / joint_vel |

## 验证

- 两文件 `py_compile` 通过。
- 64 env × 3 iteration 短训冒烟跑通，13 个奖励项全部正常更新（`track_lin_vel≈4.07` 等），无 shape/runtime 错误。
- 专项验证：接触传感器精确解析到 2 只脚，两套索引正确对齐，所有张量 shape 正确，命令/站立 mask 采样正常。

## 首轮训练观察（rsl_rl/bdx_stand_direct/2026-06-25_11-55-05，~1700 步）

机制正常但有警示信号：
- ✅ `mean_reward` 稳步升到 ~1130 仍在涨；`time_out` 从 0 升到 ~8，存活变长。
- ⚠️ **`feet_air_time` / `feet_slide` / `stand_still` 三项持续变负且越来越陡**，与 ~1400 步的 reward 跃升同步 —— 策略靠"碎步 + 脚打滑"的低质量步态涨分，**步态惩罚权重太弱压不住**。
- 量级失衡：`upright≈7.7`、`height≈5.4`、`track_lin_vel≈4.1`，而 `feet_air_time` 仅 -0.05~-0.46，差 1~2 个数量级。

## 改动 3：首轮训练后的步态权重调参

针对首轮训练观察到的"步态惩罚被巨大正奖励淹没"问题，调整权重（参考 a1：`track_lin_vel_xy_exp.weight=1.5`、flat `feet_air_time.weight=0.25`，但 a1 没有我们这种巨大的 upright/height 正常数，所以我们的步态权重需相应更大）：

| 参数 | 原值 | 新值 |
|------|------|------|
| `rew_scale_feet_air_time` | 1.0 | **8.0** |
| `rew_scale_feet_slide` | -0.1 | **-0.5** |
| `rew_scale_stand_still` | -0.5 | **-1.0** |
| `air_time_target` | 0.3s | **0.4s** |
| `experiment_name` | `bdx_stand_direct` | **`bdx_walk_direct`** |

`experiment_name` 改名同时避免行走版 checkpoint 与旧站立版目录混淆。已 `py_compile` 通过，待重训验证步态是否拉开。

## 改动 4：方向转向 —— 先训稳定站立模型（打通 sim2real）

### 背景
行走版训练（10k iter 中途）观察仍是小碎步；用户决定**先放弃行走，训一个稳定站立、不抖、不漂移的模型，把 sim2real 管线调通**。用户诊断：昨天的 stand 模型在 sim2sim 中漂移（看不出碎步但接触力 c/f 快速跳变），真机上就振荡抖动。

### 关键认识
站立 = 速度指令为 0 的行走，同一网络/观测，不冲突。之前抖/碎步不是因为"要走"，而是三个独立 bug：奖励失衡、**完全没有抗抖机制**、vx 一上来就给太大。用户选择纯站立版（`stand_still_prob=1.0`）最快拿到站死模型。

### 实施（cfg + env）
- `stand_still_prob=1.0`，`feet_air_time` 权重置 0（纯站立不鼓励迈步）。
- **新增 `contact_force_rate` 抗抖核心项**：用 `net_forces_w_history`（shape `(N,T,2,3)`）算相邻帧接触力差的范数并惩罚，直击力跳变→真机振荡。`compute_rewards` 新增该项 + `_episode_sums` 加 key。
- 加大稳定惩罚：`stand_still=-2.0`、`feet_slide=-1.0`、`lateral=-1.0`、`action_rate=-0.05`、`joint_vel=-0.02`、`ang_vel=-0.1`。
- `experiment_name` → `bdx_stand_stable`。

### 首版训练诊断（model_7900，headless 实测 push 关闭）
**关键：用 `scripts/diag_stand.py` 实测物理量，否则光看曲线会误判（曾怀疑"蹲下"）。**
- base 高度 mean **0.385m** std **0.002**，脚接触占比 **1.0**（从不抬脚），关节速度 mean 0.29 —— **没蹲下、没碎步、站姿极稳**。
- tensorboard 的 `fall=42`/`episode=187步` **全是随机推力打的**，非站不稳。
- **真正隐患**：脚钉地不动，但**接触力以 ~11N/步剧烈跳变（p95 31N、max 86N）** —— 这就是图 c/f 的力跳变，仿真因接触刚性不表现为可见运动，传真机就是电机指令高频震荡→抖动。
- **bug**：`contact_force_rate=-3e-4` 偏小约 100×（每步仅 -0.013 vs upright +1.75），训练完全无视它 → 没收敛。

### 改动 5：数据驱动调参（model_7900 诊断后）
| 参数 | 原值 | 新值 | 依据 |
|------|------|------|------|
| `rew_scale_contact_force_rate` | -3e-4 | **-3e-3** | 实测原项~44/步，×新权重≈-0.13/步，与其他惩罚可比 |
| `rew_scale_action_rate` | -0.05 | **-0.1** | 平滑电机指令，与力震荡同源 |
| `push_force_range` | (35,65)N | **(20,40)N** | 35-66%体重对10kg小机器人过猛，episode总被打断 |

重训中（`bdx_stand_stable/2026-06-25_20-03-14`，3000 iter）。早期信号改善：episode_length 187→241、fall 42→17、`contact_force_rate` 现每步~-0.13 真正生效。

## 改动 5 训练结果：抗抖项验证成功（3000 iter 完成）

`diag_stand.py` 实测对比（全部 push 关闭纯站立，硬指标）：

| 指标 | 首版 7900 | v2 最终 2999 | 改善 |
|------|------|------|------|
| **接触力变化率 mean** | 10.3 N/步 | **1.46 N/步** | **↓ 86%** |
| 接触力变化率 p95 | 27.4 N | **4.2 N** | ↓ 85% |
| 腿关节速度 mean | 0.29 rad/s | **0.156** | ↓ 46% |
| 接触力 mean | 49 N | 49 N | 一致（=自重，合理） |
| 脚接触/双支撑占比 | 1.0 | 1.0 / 1.0 | 完美不抬脚 |

训练曲线：`mean_episode_length`≈460/480（站满 8 秒）、`fall`≈0.7、`time_out`≈8.0。**结论：sim2real 抖动源（接触力高频跳变）从 10.3 压到 1.46 N/步（↓86%），站得稳、力平滑、不抖。**

⚠️ 判读陷阱：tensorboard 的 `Episode_Reward/contact_force_rate` 等是整条 episode 累加和，episode 变长（187→460）会让累加值变大，**不能据此判断每步抖动好坏**，必须用 `diag_stand.py` 实测每步量。

判读方法论坑：曾因没看 height 曲线误判"蹲下"，实测高度稳在 0.376-0.385m 根本没蹲。

## 待办 / 建议
- **导出 policy 已在 `.../2026-06-25_20-03-14/exported/`，做 sim2sim / 真机验证**（终极检验：真机抖动/振荡是否消失、是否还漂移）。
- 真机若仍有残余抖动：微调 `contact_force_rate` 再加大，或检查真机 PD 增益与 sim（stiffness=50/damping=2.5）是否匹配。
- 站立 sim2real 调通后，从该 checkpoint 续训过渡到行走（降 `stand_still_prob`、放宽 `command_vx_range`，网络不变，零过渡成本）。
- 噪声+延迟+推力鲁棒化对站立 sim2real 同样关键，全部保留。
- 诊断工具 `scripts/diag_stand.py`：headless 加载 checkpoint actor（含 obs_normalizer std 需 clamp，因命令恒 0 时 std=0），实测高度/接触力牛顿量级/抬脚频率/关节速度。
