# 2026-07-03 对照迪士尼 BDX 方案的最小修正集（路线 A）

文件：`oceanisaaclab_env.py` / `oceanisaaclab_env_cfg.py` / `scripts/diag_walk.py`

## 背景

与迪士尼 BDX（Open Duck / AWD go_bdx 复刻）逐项对照后的诊断结论：本工程 21 项手工奖励
的纯塑形范式与 BDX 的「参考轨迹模仿 + 速度命令跟踪」范式存在结构差距，但在重构为模仿
范式（路线 B，中期做）之前，有 5 个确定配错的地方可以在现框架内修正（路线 A）。
按优先级实施如下。**观测/奖励语义均有变化，必须从头重训，旧 checkpoint 全部作废。**

## 1. 侧向 vy 命令训练（左摔的直接原因）

- 此前 `_commands[:,1]` 从未采样恒为 0，且 `rew_lateral=-0.8` 无条件惩罚一切 vy²。
  sim2sim 左/右平移按键对策略是**完全分布外输入 + 训练中被惩罚的行为**。
- 本机无踝滚转关节（r1 髋 yaw、r2 髋 roll、r3 髋 pitch、r4 膝、r5 踝 pitch），侧向稳定
  只能靠髋 roll + 迈步，不训 vy 等于侧向稳定能力整体缺失。
- 改动：
  - cfg 新增 `command_vy_range = (-0.15, 0.15)`（BDX mini ±0.2 / go_bdx ±0.3，保守起步）。
  - `_reset_idx` 对所有非站立 env 采样 vy（|vy|<阈值钳 0）；参考态初速度注入同步带 vy。
  - `track_lin_vel` 改为 (vx, vy) 平面 2 维跟踪；`rew_lateral` 改为惩罚 `(cmd_vy - vy_act)²`
    偏差而非 vy 本身。vy 的头部系约定与 vx 一致（head-left = forward_vx_sign * body_y，
    180° yaw 翻转对 x/y 同时变号）。
  - `fwd_gate` / `progress` 从 vx 标量推广为平面向量投影：实际平面速度在命令方向上的
    投影比例，语义不变（原地踏步拿 0、达到命令速度拿 1），侧向命令同样受益。

## 2. 零命令冻结 gait clock（零速踏步的结构性原因）

- 此前 `_gait_phase()` 基于 `episode_length_buf` 恒定走表，零速命令下观测里的 gait clock
  仍在 0.6s 周期振荡：策略一边被 stand_still 罚踏步、一边持续看到"该迈步了"的时钟信号。
- 改动：`_get_observations` 中零速命令时 gait clock 置 (0,0)——一个与任何行走相位都可
  区分的"站立"信号（对照 BDX 零命令钳制到站立参考的做法）。

## 3. upright 项加入行走前倾标称姿态（行走姿态差的直接配置错误）

- BDX 的躯干前倾是参考步态硬约束（go_bdx 约 8°、Open Duck mini 约 3°）：无踝滚转、踝
  只有 pitch 的腿型，行走时必须把重心压到支撑面前部。旧 upright 项无条件奖励绝对竖直，
  在**主动对抗**行走所需的自然前倾。
- 改动：cfg 新增 `walk_lean_angle = 0.087`（≈5°，从 3°~8° 中值试起）。移动命令下 upright
  目标改为 `proj_g_x = forward_vx_sign * sin(lean)`（头部朝向低头前倾），站立命令仍竖直。
  倾角远低于 instability gate 阈值（0.28≈16°）与摔倒判定，无联动风险。

## 4. 领域随机化（sim2sim 一碰就摔的主因候选）

- "训练 fall=0.23 但 sim2sim 一踏步就摔"是 DR 不足的典型症状：此前只有观测噪声 + 动作
  延迟。BDX 复刻还随机化质量/摩擦/PD 增益/重力/初始姿态。
- 改动：cfg 新增 `enable_domain_rand=True` + 三个幅度区间；env 新增
  `_apply_domain_randomization()`（startup 每 env 采样一次固定参数，数千 env 覆盖分布）：
  - 质量：全 link × U(0.8, 1.2)（BDX ×[0.5,1.5]，先保守，验证后再放大）。
  - PD 增益：各执行器 stiffness/damping 独立 × U(0.8, 1.2)，implicit actuator 同步写回 sim。
  - 摩擦：碰撞体 static/dynamic friction × U(0.7, 1.3)（对照 BDX），经 `root_physx_view`；
    后端不暴露该 view 时告警跳过。

## 5. 推扰重开：间隔 5~10s（晃悠姿态的诱因）

- 07-02 曾全关推力隔离验证。对照结论：问题不在推力本身而在密度——旧间隔 1~2s 使 8s
  episode 里机器人永远处于"刚被推/即将被推"状态，学出"常态微失衡随时准备迈步"的晃悠
  姿态是必然（Open Duck 推扰是每 5~10s 一次）。
- 改动：`enable_random_push=True`，`push_interval_s=(5.0, 10.0)`，力度沿用 (8,20)N 起步
  + 原课程爬到 (20,40)N。无扰稳定站走成为占绝对主导的训练状态，推扰只是偶发事件。

## 6. 低速段 + 双脚接触观测

- `command_vx_range` 从 (0.10, 0.25) 改为 (0.0, 0.25)（课程起点 (0.0, 0.18)），补上
  0~0.10 低速微调命令空洞；采样后 |vx|<move_command_threshold 钳 0，站立/移动语义清晰。
- 观测加入双脚接触布尔量（真机脚底开关可得；BDX 观测集也有），对步态相位对齐帮助大。
  **observation_space 41 → 43**（顺序：ang_vel 3 + proj_g 3 + cmd 3 + gait_clock 2 +
  feet_contact 2 + Δjoint_pos 10 + joint_vel 10 + last_action 10）。

## 诊断脚本

- `diag_walk.py` 新增 `--vy` 参数与侧向跟踪比输出（vy 方向跟踪专项检查）。

## Step-by-Step TODO（PC 端 Isaac Lab 环境）

1. **[冒烟测试]** 随机动作跑通新观测/奖励（确认无 shape/NaN 报错、DR 无 API 报错）：
   ```bash
   ./_isaaclab/isaaclab.sh -p scripts/random_agent.py --task=Ocean-BDX-Stand-Direct-v0 --num_envs 64
   ```
2. **[从头重训]**（观测 41→43、奖励语义全变，不 resume）：
   ```bash
   ./_isaaclab/isaaclab.sh -p scripts/rsl_rl/train.py \
     --task Ocean-BDX-Stand-Direct-v0 --num_envs 8192 --max_iterations 20000 --headless
   ```
3. **[iter~3000 中途体检]** `analyze_tb.py` 看 track_lin_vel / lateral / fall 曲线；
   `diag_walk.py --vx 0.2` 与 `--vx 0 --vy 0.1`（新增侧向专项）与 `--vx 0 --vy 0`
   （零命令"时钟静默 + 钉地"专项：接触比例应≈1.0、离地事件≈0）。
4. **[sim2sim]** 重点验证：左右平移按键（此前分布外）、站立不踏步、推扰恢复。

## 已知遗留 / 后续（路线 B，中期）

- 范式升级为参考模仿：Placo ZMP 生成 (dx,dy,dθ) 参数化参考步态库，奖励改为关节角
  L2 匹配主导 + 接触时序匹配 + 基座姿态/速度匹配，删除 fwd_gate / instability gate /
  phase_contact / swing_contact / single_support / feet_clearance / feet_air_time /
  stand_still 整个联动栈——以上所有打地鼠问题的统一根治。
- DR 幅度（质量/PD 0.8~1.2）验证稳定后向 BDX 的 ×[0.5,1.5] 靠拢；可补重力偏差与
  初始朝向随机化。
- 抬脚目标 0.05m 偏高（go_bdx 全尺寸 0.025m、mini 0.04m），下轮可降到 0.03~0.04。

## Bugfix：训练启动即崩——DR 摩擦随机化的 warp/torch API 误用

现象：`train.py` 一启动就在 env `__init__` 报错崩溃：
```
File ".../oceanisaaclab_env.py", line 142, in _apply_domain_randomization
    friction_scale = sample_uniform(lo, hi, (materials.shape[0], 1, 1), materials.device)
TypeError: rand() received an invalid combination of arguments - got (int, int, int, device=Device)
```

根因（`_apply_domain_randomization` 第 3 步摩擦随机化，两处 API 用法错）：
1. **`physx_view.get_material_properties()` 返回的是 warp 数组而非 torch 张量**，其 `.device`
   是 warp `Device` 对象。`sample_uniform` 内部 `torch.rand(*size, device=device)` 收到 warp
   Device 就报 `invalid combination of arguments`。质量/PD 两步用 `self.device`（合法 torch
   设备）所以没事，唯独摩擦这步误用了 `materials.device`。
2. **写回 `set_material_properties(materials, torch.arange(...))` 直接传 torch 张量**同样会失败
   ——PhysX tensor API 要的是 warp 数组 + CPU int32 的 env_ids。

修正（对齐 Isaac Lab 官方 `randomize_rigid_body_material`，见 `envs/mdp/events.py:230`）：
- 顶部新增 `import warp as wp`。
- 读取：`materials = wp.to_torch(physx_view.get_material_properties())` 转成 torch 张量（材质
  buffer 在 CPU 上，故 `materials.device` 变为合法的 cpu 设备，`sample_uniform` 正常）。材质
  shape `(num_envs, num_shapes, 3)`=static/dynamic friction + restitution，`friction_scale`
  `(num_envs,1,1)` 广播缩放前两列（两种摩擦），restitution 不动。
- 写回：`set_material_properties(wp.from_torch(materials.contiguous(), dtype=wp.float32),
  wp.from_torch(env_ids_cpu_int32, dtype=wp.int32))`。

要点/教训：Isaac Lab 里凡是走 `root_physx_view` / `root_view` 的底层 PhysX tensor API
（material、部分 mass/inertia 接口），返回值是 **warp 数组**，须 `wp.to_torch` 进、
`wp.from_torch` 出，且 env_ids 常要求 **CPU int32**；不能像上层 `robot.data.*`/`set_*_index`
那样直接用 torch + `self.device`。改动纯属 DR 实现修复，不影响任何观测/奖励语义。

验证：`train.py --num_envs 64 --max_iterations 2` 冒烟 2 iter 正常跑完，无 traceback/NaN/shape
报错，DR（质量/PD/摩擦）全部生效。

---

# 路线 B（Plan B）：BDX 式参考步态模仿——实现与 TODO

文件：`scripts/gen_reference_gait.py` / `assets/gaits/reference_gait.npz` /
`reference_gait.py` / `oceanisaaclab_walk_env.py` / `oceanisaaclab_walk_env_cfg.py` /
`__init__.py`（注册）/ `agents/rsl_rl_ppo_cfg.py`（WalkPPORunnerCfg）

## 方案（对照 Open Duck / AWD go_bdx）

BDX 复刻用 Placo ZMP walk-pattern generator 离线生成按 (dx, dy, dθ) 参数化的周期参考
步态库，训练时按命令插值取参考帧，奖励以关节角匹配为主导。本工程无 Placo 依赖，用
纯 numpy 复刻同等产物：URDF 解析双腿 5-DOF 运动链 → 数值 FK → 运动学步态公式（支撑
脚世界系静止 + 摆动脚余弦摆动 + 抛物线抬脚 0.035m + 躯干随 vx 前倾 ≤5° + 侧向重心
sway 0.015m）→ 阻尼最小二乘 IK 解 10 个腿关节角。

- 命令网格：vx∈{-0.25..0.25}×5、vy∈{-0.15,0,0.15}、wz∈{-0.8..0.8}×5，每命令 48 相位
  帧；零命令网格点是常量微蹲站立帧（h=0.36m），命令插值自然给出「命令→0 步幅→0」。
- 头部前向约定与 forward_vx_sign=-1 完全一致；相位约定与 env `_gait_phase()` 一致
  （右脚 φ∈[0,0.5) 支撑、左脚反相）。
- 库质量：全库最大 IK 位置残差 2.15mm；参考关节速度峰值 4.9 rad/s（限 6）；姿态权重
  0.05（本腿无踝滚转，sway/转向时脚底无法严格水平，高权重会牺牲位置精度到 ~15mm）。

## 新任务 Ocean-BDX-Walk-Direct-v0（模仿范式）

- 继承路线 A 环境全部基础设施：43 维观测不变（sim2sim 链路不动）、命令采样/课程、
  推扰、DR、动作延迟、观测噪声全部保留。
- 奖励整体替换为模仿式（正奖励为主，exp 核）：关节角匹配 3.0（主导）+ 关节速度匹配
  0.3 + 接触时序匹配 1.0 + 基座高度匹配 0.5 + 基座姿态匹配 1.0（行走前倾/站立竖直
  全部由参考决定）+ 平面速度跟踪 1.5 + yaw 跟踪 0.75 + alive 0.25 + action_rate
  -0.05 + feet_slide -0.2。**删除整个手工塑形联动栈**：fwd_gate / instability gate /
  phase_contact / swing_contact / single_support / feet_clearance / feet_air_time /
  stand_still / no_fly / progress / upright / contact_force_rate。
- RSI（参考态初始化，DeepMimic 关键技巧）：90% env 从随机相位参考帧出发（关节角
  +0.03rad 噪声、参考高度/俯仰、参考基座速度），绕过「从静止起步」最难阶段。
- target_base_height 0.42 → 0.36（参考步态微蹲高度，留 IK 上下工作空间）。
- 与路线 A 观测/奖励语义不兼容，需从头训练（旧 checkpoint 不可 resume）。

## Step-by-Step TODO（PC 端 Isaac Lab 环境）

- [x] 参考步态生成脚本 `scripts/gen_reference_gait.py`（URDF FK + DLS IK）
- [x] 步态库 `assets/gaits/reference_gait.npz`（75 命令 × 48 相位，残差 2.15mm）
- [x] 运行时采样器 `reference_gait.py`（命令三线性 + 相位环形插值，纯张量、已单测）
- [x] 模仿环境 `oceanisaaclab_walk_env.py` + cfg（模仿奖励 + RSI）
- [x] 注册 `Ocean-BDX-Walk-Direct-v0` + `WalkPPORunnerCfg`（experiment=bdx_walk_imitation）
- [x] **[冒烟]** 随机动作跑通（确认库加载、RSI、奖励无 shape/NaN 报错）：
  ```bash
  ./_isaaclab/isaaclab.sh -p scripts/random_agent.py --task=Ocean-BDX-Walk-Direct-v0 --num_envs 64
  ```
- [x] **[参考回放体检]** 见下文「参考回放体检 + RSI 四元数 bug」小节：已实现
  `scripts/diag_reference_playback.py`，并借此揪出+修掉一个 RSI 四元数翻转 bug。
- [ ] **[从头重训]**：
  ```bash
  ./_isaaclab/isaaclab.sh -p scripts/rsl_rl/train.py \
    --task Ocean-BDX-Walk-Direct-v0 --num_envs 8192 --max_iterations 20000 --headless
  ```
- [ ] **[iter~2000 中途体检]** `analyze_tb.py`：imit_joint_pos 应稳步升向 ~2.5+/3.0、
  imit_contact 升向 ~0.9+、fall 下降；`diag_walk.py --vx 0.2 / --vy 0.1 / 零命令`。
- [ ] **[sim2sim]** 重点：站立不踏步、W/S/A/D 全向、推扰恢复、行走姿态（前倾自然度）。
- [ ] **[后续调参方向]** 若步态贴参考但速度跟踪差 → 提 track_lin_vel 权重或放宽
  imit_joint_pos_sigma；若脚步拖地 → 库里加大 FOOT_CLEARANCE 重生成；稳定后 DR 幅度
  向 BDX ×[0.5,1.5] 靠拢；可尝试把 episode 起始命令课程关掉（模仿范式通常不需要）。


---

# 2026-07-03（追加）参考步态生成器迁移到 placo + 可视化 + play 脚本

文件：`scripts/gen_reference_gait.py`（重写）/ `scripts/play_reference_gait.py`（新增）/
`oceanisaaclab_walk_env_cfg.py` / `assets/gaits/reference_gait.npz`（重新生成）

## 背景

三个诉求：① 生成的参考轨迹此前只能看 IK 残差数字，无法直观确认好坏；② 步态参数
散落为脚本顶部常量，不便调整；③ 纠正基座高度约定——URDF 关节全零姿态**就是** BDX
标准站立姿态（屈膝微蹲、腿部有伸直角度预留），此前参考库把基座又压到 0.36m 属于
理解偏差，不需要再降低身体高度。

## 1. IK 后端换 placo（Open Duck / BDX 复刻同款）+ meshcat 可视化

- 手写 numpy DLS IK 替换为 `placo.KinematicsSolver`（双脚 6D FrameTask，关节软限位
  0.9 因子由求解器强制满足）。URDF mesh 的 `package://` 路径自动重写到
  `assets/meshes`。依赖：`pip install placo meshcat`。
- 脚姿态任务权重从 0.05 调到 0.0025（placo 的权重不像旧 DLS 那样平方作用，0.05 会
  把位置精度牺牲到 ~9mm）：全库最差 IK 位置残差 1.73mm（旧版 ~2mm），脚底 z 轴对齐
  仍 >0.997（近似水平）。
- `--viz` 生成时逐命令用 meshcat 真实时间回放（基座抬到参考高度 + 前倾，双脚
  frame 标记）；`--viz-cmd "0.25,0,0" --viz-loops 5` 只反复看某个命令。

## 2. 步态参数集中为 GaitParams（全部命令行可覆盖）

`gait_period`（周期，须与 cfg.gait_cycle_period 一致）/ `gait_duty`（占空比）/
`base_height`（<=0 自动取全零姿态站立高度）/ `foot_clearance`（**抬腿高度**）/
`lateral_sway`（侧向重心转移）/ `walk_lean_angle`（前倾角）/ `vx_max`·`vy_max`·`wz_max`
（命令网格）等，dataclass 逐字段 docstring 说明。**步幅**不是独立参数，由
`vx × gait_period × gait_duty` 推出（满速 0.25×0.3=7.5cm），日志逐命令打印步幅。

## 3. 新增 scripts/play_reference_gait.py 播放步态库

- 对任意 (vx, vy, wz) 命令做与训练端 `reference_gait.py` 完全一致的三线性 + 相位
  环形插值采样（numpy 版），可验证网格点之间的插值质量。
- 基座按命令在世界系真实行进（积分 vx/vy/wz），绿/红小球标记双脚参考接触状态；
  `--in-place` 原地回放、`--speed 0.25` 慢放、`--info` 只看库参数。
- python scripts/play_reference_gait.py --vx 0.25
- http://127.0.0.1:7000/static/
## 4. 基座高度纠正：全零姿态即标准站姿，不再下蹲

- 参考库基座高度从硬编码 0.36m 改为默认自动取 URDF 全零姿态 FK 站立高度
  **0.385m**；站立参考帧因此精确等于全零关节角（此前站立帧要屈膝凑 0.36）。
- `cfg.target_base_height` 0.36 → 0.385 同步。
- npz 已按新参数重新生成（格式/键/形状与旧版完全一致，`gait_period` 0.6 不变，
  不影响 env 加载）。**奖励里的参考量变化，需从头重训。**

## 5. placo 脚本运行环境修正（依赖/版本兼容）

`gen_reference_gait.py` / `play_reference_gait.py` **只依赖 placo+meshcat+numpy，不依赖
Isaac Sim，必须用系统 `python3` 跑，不能用 `isaaclab.sh -p`**（约定的例外）：

- 用 `isaaclab.sh -p` 跑会报 `pinocchio ... No to_python converter for boost::none_t`
  ——placo/pinocchio 装在 Isaac 自带 python，其 C++ 绑定与 Isaac 自带 boost 符号冲突。
  系统 python3 里 placo 可干净导入。
- 系统 python3 仍有二级冲突：可视化链 `placo_utils→trimesh→scipy`，apt 系统 scipy
  1.11.4（要求 numpy<1.28）与 `~/.local` 的 numpy 2.3.5 ABI 不兼容
  （`numpy.dtype size changed`）。修复：`pip install --user --break-system-packages
  "scipy>=1.13"`（装了 1.18.0 到 `~/.local` 盖住 apt 旧版，与既有 numpy/placo/meshcat
  同目录，可逆）。
- 正确跑法：`python3 scripts/play_reference_gait.py --vx 0.2`（`--info` 只读 npz，
  看动画开终端打印的 meshcat URL）。已验证回放正常。

## 6. 参考回放体检 + 修掉 RSI 四元数翻转 bug（TODO「参考回放体检」）

新增 `scripts/diag_reference_playback.py`：在 walk env 里把策略动作直接固定成参考
关节角（`action = (ref_joint_pos - default) / action_scale`，腿部 default 恒 0），
关掉一切扰动（推力/DR/噪声/延迟），开环回放参考步态，统计**首次摔倒前**（逐 env
mask，避免摔后乱翻污染）的：存活时长、头前向/侧向/yaw 跟踪、实测vs参考接触一致度、
摆动脚脚底间隙；并带「reset 后未 step」的 RSI 探针与前 40 步终止原因分解。

用 `isaaclab.sh -p scripts/diag_reference_playback.py --vx 0.2` 跑（纯统计）；加 `--viz kit`
开 GUI 窗口，实时节奏播放、统计跑完后持续循环回放，肉眼观察参考步态开环回放（关窗/Ctrl-C 退出）。

**与 `play_reference_gait.py` 的区别（两者互补）**：`play_reference_gait.py`（系统 python + meshcat）
是**纯运动学**回放——看「库里存的参考长什么样」（关节角/接触/前倾，无物理）；本脚本
（isaaclab python + 物理）把参考角当动作喂进 PD+物理**开环**回放——看「参考喂进物理能不能走」
（动力学可行性）。

### 揪出的 bug：RSI 基座四元数被写翻（致命，会废掉路线 B 训练）
- 首轮体检：**中位 0 步就摔、64 env 无一存活**；终止原因分解显示全是 `not_upright`；
  RSI 探针（reset 后未 step）显示 **61/64（≈rsi_prob 0.9）已 >49° 倾倒、proj_g_z→+1
  （绕 X 翻 180° 底朝天）**，但 base_h 正常 0.39。非 RSI 的 10% env（沿用 default 朝向）
  正立——锁定是 RSI 手工构造四元数的锅。
- 根因：RSI 里 `root_pose[:,3:7] = (cos(半角),0,sin(半角),0)` 假设 wxyz 顺序，但该管线
  root_pose 四元数实为 **xyzw**（cfg `init rot=(0,0,0,1)` 正是 xyzw 单位四元数）→ 手写
  等于把 cos 放进 x 分量 → 绕 X 翻 ~180°；且手写还丢弃了 default 的朝向。库本身没问题
  （`base_pitch` 仅 ±5°、`proj_g_z`∈[-1,-0.996]、`lin_vel_b` 全小量）。
- 修复（`oceanisaaclab_walk_env.py` `_reset_idx`）：RSI **不再手工覆盖四元数**，朝向直接
  沿用 `default_root_pose`（与非 RSI env / 路线 A spawn 一致，保证正立方向对）。±5° 的
  初始前倾对 RSI 可忽略，前倾姿态由 `imit_orient` 奖励在 episode 中学到。
- 遗留提示（次要，量级小未细究）：RSI 用 `root_vel[:, :3] = ref["lin_vel_b"]`（body 系）
  当世界系速度注入，若 default spawn 有 yaw 则方向有偏；vx≤0.25 量级小，不致摔，重训
  观察后再定是否精确化。

### 修复后体检结果（--vx 0.2, 64 env, 无扰动）
- RSI 探针：reset 后 `not_upright 0/64`、proj_g_z=+1.0 正立 ✓。
- **首次摔倒 平均 0.70s / 中位 0.68s**（>1 个步态周期 0.6s），前 11+ 步零终止。
- 头前向 vx 跟踪比 **0.75**（开环无反馈能到七成即算方向/步幅正确）；摆动脚脚底间隙
  **0.021~0.035m ≈ 库里 0.035** ✓。
- 判读：**参考步态库动力学合理**——朝命令方向走、抬脚高度对、撑过一个多步态周期后
  才因「双足无踝滚转 + 开环无平衡反馈」自然倾倒（预期内，闭环 RL 策略负责补平衡）。
  库无需重新生成；可以进入从头重训。（注：`joint_vel` 峰值 7.31 略超限 6，仅被执行器
  限幅，影响轻微，下轮生成可顺带压一下。）

## 7. 「完全不抬脚」根因：action_scale 太小，动作够不到参考摆动角

现象：路线 B 训到 ~300 iter，sim2sim 里能稳定站立、前后左右/QE 命令都响应、身体按步态
节奏扭动，但**摆动脚完全不抬**。

排查：不是抬脚奖励/惩罚的问题（路线 B 无独立抬脚项，抬脚靠 `imit_joint_pos` 匹配参考位姿
涌现，奖励结构正常）。根因是**动作幅度卡死**：
- 动作管线 `关节目标 = default_leg(0) + action_scale * action`，action 天然 ~[-1,1]，
  `action_scale=0.25`（继承基类）→ 关节目标只能到 **±0.25 rad**。
- 但参考步态摆动峰值（vx=+0.25）：**膝 leg_[lr]4≈0.41rad、踝 leg_[lr]5≈0.31rad**、
  髋pitch leg_[lr]3≈0.25rad，全库 abs max≈0.5rad。抬脚最关键的膝/踝**物理上够不到**参考
  摆动角 → 一到 0.25 饱和 → 抬不起脚；策略只能匹配小幅髋 roll/yaw(≤0.07rad) → 「身体扭
  但不抬脚」。
- 为何开环回放（第 6 节）却能看到抬脚：回放脚本 `action=ref/action_scale`（膝需 action≈1.63）
  且 unwrapped env 不 clip，照单全收；但训练策略动作分布在 ~[-1,1]，够不到 1.63。这个
  不对称正好暴露了 bug。

修复（`oceanisaaclab_walk_env_cfg.py`）：`action_scale` 0.25 → **0.5**（仅路线 B override，
不动路线 A——其浅步态在 0.25 内够用）。这样 action∈[-1,1] 覆盖参考 ±0.5rad，膝峰值 0.41→
action 0.82，留余量给平衡修正。冒烟：walk env 正常构建步进，无报错。

连带：
- **须从头重训**（动作语义变了，旧 checkpoint 作废）。
- **sim2sim/onnx 部署侧 `action_scale` 同步 0.5**（`oceanbdx/config/oceanbdx.yaml` 的
  `policy.action_scale` 与 `sim2sim.action_scale`，已同步），否则真机/mujoco 下发的关节
  幅度与训练对不上。
- 重训 TB 重点盯 `Episode_Reward/imit_contact`（不抬脚卡~0.5，正常迈步升向~0.9+）与
  `imit_joint_pos`（升向~2.5+/3.0）；辅以 `fall`/`Policy/mean_std`/`Loss/value` 看有无训崩。

## 8. 移除观测里的双足接触量：本项目不装足底接触开关（observation 43 → 41）

文件：`oceanisaaclab_env.py`（`_get_observations`）/ `oceanisaaclab_env_cfg.py` /
`oceanisaaclab_walk_env.py`·`_cfg.py`（docstring）/ `README.md`

### 背景 / 决策
- 之前对照 BDX 在观测第 41、42 位加了 `feet_contact`（左右足接触布尔，取自 contact_sensor
  实测），意味着真机必须有足底接触检测。查证：BDX/Open Duck Mini v2 真机确有物理足底开关
  （Open Duck 是舵机+GPIO 微动开关的简化方案；全尺寸 BDX 资料称为多点位以应对不平地面），
  runtime 把开关值拼进策略观测。
- 但本项目**决定直接不装足底接触开关**：全尺寸 BDX 的多点位方案硬件太复杂，Open Duck 的
  单点开关位置/可靠性也不确定。综合判断去掉是低风险：
  1. 相位信息已由 `gait_clock`（sin/cos of phase）承载，`feet_contact` 只额外提供
     “指令接触 vs 实际接触”这一反馈通道，去掉不丢相位。
  2. 当前训练里 `feet_contact` 是**干净投入**（`enable_obs_noise` 只噪 ang_vel/proj_g/
     joint_pos/joint_vel，不噪接触）——干净 sim 接触喂给无/不可靠开关的真机是 sim2real 陷阱，
     无它反而更稳。
  3. 平地定常行走本就不依赖它（proprioception + IMU + 相位时钟足够），仅外扰恢复锐度略降。

### 改动
- `_get_observations`：删除 `feet_contact` 计算与 `torch.cat` 里的对应项。新观测 41 维：
  `ang_vel3 + proj_g3 + cmd3 + gait_clock2 + Δjoint10 + jvel10 + last_action10`。
- `observation_space` 43 → 41。docstring/README 的“43 维”同步为“41 维，无足底接触量”。
- **接触仍用于奖励**（路线 A 的 phase_contact/swing_contact 等、路线 B 的 imit_contact），
  那是训练信号而非策略输入，真机不需要任何足底检测。`_feet_contact_ids`/contact_sensor 保留。

### 连带
- **须从头重训**（观测维度/布局变，旧 checkpoint 作废）；`ast` 解析通过，待冒烟。
- 部署侧 obs 拼装同步去掉接触位；注意 `scripts/real/bdx_stand_runner.py` 的
  `build_observation` 是 6/23 的旧 stand 脚本（39 维，连 gait_clock 都没有），与当前 41 维
  已不一致，需要真机跑行走策略时再整体对齐（本次未动，属遗留）。

### 部署工程 `/home/ocean/oceanbdx/` sim2sim + 真机观测对齐（同步 43 → 41）
- `config/oceanbdx.yaml`：`num_obs` 43 → 41，注释更新为
  `ang_vel3+proj_g3+cmd3+gait_clock2+Δq10+dq10+last_act10`。
- `sim2sim/mujoco_sim.py`：`Policy.step` 去掉 `feet_contact` 参数与 obs 拼装里的接触量；
  两个调用点（`probe_policy` 探针、主 RL 循环）同步去掉实参。主循环里原来喂观测的
  `foot_contact_forces()` 调用一并删除——**真机（--real）模式因此不再依赖 mujoco 接触力**，
  符合「真机无足底传感器」的现实。`foot_contact_forces()` 本身保留（仅用于调试打印/推力日志）。
- sim2sim 与真机共用同一份观测构造（`real_bridge.py` 只做电机 UDP 桥接，不建观测；真机
  `--real` 复用 PolicyRunner 主循环的 `Policy.step`），改一处即同时覆盖。
- `sim2sim/export_ckpt_onnx.py` 的 `obs_dim` 从 checkpoint 自动推断，重训后重导出自动变 41，无需改。
- 连带：现有 `policy/policy.onnx` 是 43 维，加载会 shape 不匹配，须重训 + 重导出；
  `action_scale` 已同步为 0.5（与路线 B 一致）。

## 9. 路线 B 关闭命令幅度课程 + 推力课程起爬点提前

文件：`oceanisaaclab_walk_env_cfg.py`

- **命令幅度课程关掉**（`enable_command_curriculum = False`，仅路线 B override）：模仿范式
  不需要「由慢到快」的爬坡。RSI（90% env 从随机相位参考帧出发）已绕过「从静止起步」最难
  阶段；参考库对全命令网格 vx∈{-0.25..0.25} 都有逐帧参考、零命令点即站立帧、插值天然给出
  「命令→0 步幅→0」，任何速度下学习信号都密集且良定义。开着反而前期把 vx 压在起点
  (0,0.18)、高速参考帧长期见不到，拖慢高速段收敛（DeepMimic/AWD 类模仿通常全命令分布直训）。
- **推力课程保留但起爬点提前**（`push_curriculum_start_step` 120_000 → 60_000）：抗扰鲁棒性
  模仿一样需要，「先学会模仿再叠加大推力」分阶段逻辑仍成立；但不再有命令课程走满作为锚点，
  且 RSI 收敛快，提前到 ≈2500 iter 让抗推早点介入。力度区间 (8,20)→(20,40)N 不变。
- 影响：路线 B 一开局 vx 即在全范围 (0,0.25) 采样。重训中途体检留意 `track_lin_vel` 早期
  可能因高速命令偏低（正常，模仿靠参考帧带会跟上）。路线 A 不受影响。`ast` 解析通过，待冒烟。
