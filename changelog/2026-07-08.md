# 2026-07-08 阶段1:walk 策略加脖子+头部命令(10→14 DOF,论文头部命令复刻)

前置:07-07 三项奖励地形改动后(k30/survival2/entropy0.005 + leg_joint-25/contact1.5/
rsi0.8),行走策略训到 21000 iter 成功——所有命令能响应、前进步态优秀(sim2sim 站住+
前进步频步幅前倾都到位),后退会摔(2w vs 论文 10w,后退难模式没练够)。不带头部的
route B 基本成立。本次按论文完整复刻头部命令,把脖子纳入策略控制。

## 决策(与用户确认)
- 头部命令 **4-DOF**:(Δh_head 头高, pitch 点头, yaw 摇头, roll 歪头),用满脖子 4 关节。
- 顺序:先给 walk 加脖子+头(本次),重训验证后再建独立站立(perpetual)模型(阶段2)。

## 关键设计:脚 IK 与首 IK 解耦
base 固定时脖子在 base 之上、不影响脚 IK。故头部命令**不做成步态库网格维度**(会爆炸),
单独生成一张 4-DOF 头命令→脖子角映射表,运行时四线性插值。现有腿部步态库不用重生成。

## 探明的脖子运动学(placo + URDF)
neck_n4_link 是头。4 关节有效轴(base 系):n1/n2 矢状面对=**头高 Δh + pitch 耦合**、
n3=**yaw**、n4=**roll**。yaw/roll 独立、映射完全线性单调;**头高权限弱**(与 pitch 共用
n1/n2),压到 ±2cm + 连续 warm-start(蛇行遍历)保证映射单调。横向头移动不可实现。

## 改动清单
1. **新增 `scripts/gen_neck_head_map.py`**:placo base 固定 + 头部姿态(3D)+ 高度(仅 z)
   任务(4 约束匹配 4 关节;不钉头 x,y——4 连杆转头必然平移,钉死会矛盾)。姿态偏移
   **base 系左乘** `Rz(yaw)Ry(pitch)Rx(roll)@R0`(头 link 局部系转了 ~90°,右乘会错配关节)。
   4-DOF 网格 5⁴=625,蛇行+连续 warm-start。已生成 `assets/gaits/neck_head_map.npz`
   (worst 头高残差 83mm,仅出现在全轴同时打满的角落;单轴/中等命令 <mm 级)。
2. **`reference_gait.py`**:加 `NeckHeadMap` 类(4D 四线性插值,复用 `_grid_coords`),
   `sample(head_cmd(N,4))→脖子参考角(N,4)`。单测通过(zero→zero、yaw/roll 精确、钳制正常)。
3. **`oceanisaaclab_walk_env_cfg.py`**:`action_space` 10→**14**、`observation_space` 57→**77**、
   `state_space` 59→**79**;加 `neck_head_map_path`、`neck_action_joint_ranges`(0.8,0.8,1.2,0.7)、
   头命令 4 轴采样范围(dh±0.02/pitch±0.5/yaw±1.0/roll±0.6)、`head_command_scale`(20,1,1,1);
   加脖子奖励权重(neck_joint_pos **-100** 论文原值/neck_joint_vel -1/neck_action_rate -5/
   neck_action_acc -5);脖子保持位置伺服(stiffness50)但不再锁死。
4. **`oceanisaaclab_walk_env.py`**:加载 NeckHeadMap;动作切片(前10腿力矩直驱/后4脖子位置
   伺服);`_pre_physics_step` 脖子目标=default±range clamp 软限位;`_apply_action` L384
   **去掉脖子锁死**,改喂学习目标;obs 追加 neck q(4)/qd(4)/head_cmd(4);奖励加 4 个脖子项
   (腿的 action_rate/acc 改为只取腿切片,脖子单独加权);`_reset_idx` 采样头命令 + 脖子目标/
   RSI 初始角=参考角。
5. **`oceanisaaclab_env.py`**(基类 reset):L484 `_processed_actions` 赋值按腿宽度切片
   (`[:, :n_leg]`),避免 action_space>10 时形状不匹配(route A 行为不变)。

## 观测布局(77 维)
p_pf2 + yaw_pf2 + lin_vel3 + ang_vel3 + q_leg10 + q_neck4 + qd_leg10 + qd_neck4
+ a_{t-1}14 + a_{t-2}14 + phase谐波4 + cmd3 + head_cmd4 = 77;critic +摩擦+质量 = 79。

## 验证
- 全文件 py_compile 通过;NeckHeadMap 单测通过。
- 冒烟 16 env × 2 iter headless **通过**:环境创建→步进→学习完整,18 个奖励项(含新增
  neck_joint_pos/vel/action_rate/acc)正确统计,action std 0.30,无形状/索引错误。

## 待办
- **从头重训**(obs/action 布局变,旧 checkpoint 失效)。保留了 07-07 调好的奖励地形参数,
  大概率能复现前进步态。~20k+ iter。验证:前进步态仍长出 + 脖子跟随头命令(play 给头命令看
  头动)。风险:14-DOF 下前进步态需重新长出;回退方案=脖子改前馈 IK 直驱(不进 RL action)。
- sim2real 侧(oceanbdx)需同步:obs 77 维布局、脖子进 action、头命令通道、neck_head_map 复刻。

---

# 阶段2:站立(perpetual)独立模型(论文 divide-and-conquer 第二个策略)

行走模型(阶段1)训练中,头部响应正确、行走正常。按论文再建一个**独立的站立策略**
π(a|s, g_perp),无相位、脚不迈步,命令 g_perp=(Δh_head,Δθ_head,h_torso,θ_torso)(式5)。
全部新文件,不碰正在训练用的 walk_env/cfg。

## 站立命令(g_perp,8 维)
- 躯干 4-DOF:(h_torso 高度偏移, pitch 前后倾, yaw 偏航, roll 侧倾)
- 头部 4-DOF:(Δh, pitch, yaw, roll),复用 neck_head_map

## 观测布局(74 维,无相位)
p_pf2 + yaw_pf2 + lin_vel3 + ang_vel3 + q_leg10 + q_neck4 + qd_leg10 + qd_neck4
+ a_{t-1}14 + a_{t-2}14 + torso_cmd4 + head_cmd4 = 74;critic +摩擦+质量 = 76。
(相比行走 77:去掉 phase 谐波4、cmd3→torso4,net −3)

## 新增文件
1. **scripts/gen_stand_pose.py**:placo 双脚固定地面锚点 + 躯干处于命令高度/朝向,解 10 腿角。
   base 系脚目标 = R_bw @ (p_foot_world − [0,0,h_torso]),R_bw=R_torso^T。躯干 4-DOF 网格
   5⁴=625,蛇行+连续 warm-start。生成 `assets/gaits/stand_pose.npz`(base_height=0.385,
   worst foot 残差 36mm 仅在升高极限)。腿角随高度/pitch 单调平滑。含 `--viz`/`--viz-only`
   meshcat 逐轴扫掠。
2. **reference_gait.py 加 `StandPose`**:躯干命令 4-DOF 四线性插值→腿角(N,10),暴露 base_height。
   单测通过(零命令≈标称站姿、蹲下屈膝、前倾左右对称、钳制正常)。
3. **oceanisaaclab_stand_env_cfg.py**:`OceanisaaclabStandEnvCfg(OceanisaaclabWalkEnvCfg)`。
   obs 74/state 76/action 14;stand_pose_path;躯干命令范围(h[-0.05,0.02]/pitch±0.25/
   yaw±0.35/roll±0.18)+ torso_command_scale(10,1,1,1)+ stand_zero_command_prob 0.1;
   站立奖励权重(加大 torso_orient 权重2、新增 torso_height 权重2/k200,leg_joint_pos -15,
   脖子项同行走)。
4. **oceanisaaclab_stand_env.py**:`OceanisaaclabStandEnv(OceanisaaclabWalkEnv)`。覆盖
   `_get_observations`(去 phase、命令换 g_perp)、`_get_rewards`(静态姿态:躯干位置在双脚
   中心/朝向跟命令/高度跟命令、线角速度→0、腿角模仿站立参考、脖子模仿头命令、双脚接触、
   正则、存活)、`_reset_idx`(复用父类 reset 再覆盖:loco 命令置0、采样躯干命令、从标称站姿
   default 出生正立零速、脖子到头命令参考角)。执行器/外乱/DR/path frame/脖子通路全继承。
5. **rsl_rl_ppo_cfg.py 加 `StandPPORunnerCfg`**:继承 Walk,experiment_name="bdx_stand_perpetual",
   entropy_coef 调回论文 0(站立无迈步局部最优)。
6. **__init__.py 注册 `Ocean-BDX-StandPaper-Direct-v0`**。

## 验证
- 全文件 py_compile 通过;StandPose 单测通过;站立库单调平滑。
- **冒烟 16env×2iter 通过**:19 个奖励项(含 torso_height + 4 neck 项)正确统计,无形状错。

## 站立训练命令
```
./_isaaclab/isaaclab.sh -p scripts/rsl_rl/train.py --task Ocean-BDX-StandPaper-Direct-v0 --num_envs 8192 --headless
```
两个模型独立训练、运行时按需切换(论文图9)。站立 sim2sim/sim2real 部署侧待后续同步
(obs 74 维、无相位、g_perp 命令通道)。
