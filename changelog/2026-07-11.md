# 2026-07-11

## model_39200 左右脚、后退和头部运动诊断

固定零头命令、关闭噪声/扰动/延迟，对
`logs/rsl_rl/bdx_walk_imitation/2026-07-10_18-46-07/model_39200.pt` 做 32-env
IsaacLab 诊断，并用其完全一致的 ONNX 在 MuJoCo 中复测。

- IsaacLab 前进 `vx=0.15`：实际 `0.148m/s`；右脚摆动占比 `20.4%`、间隙 p50
  `3.23cm`，左脚仅 `9.0%`、p50 `0.60cm`，左脚 `97%` 摆动样本低于 `1.5cm`。
- IsaacLab 后退 `vx=-0.15`：实际 `-0.132m/s`；右/左摆动占比仅 `3.2%/9.4%`，
  接触匹配降至 `1.318/2`。
- IsaacLab 腿部 applied torque p99 仅约 `3.4-4.1Nm`，电机模型限幅/摩擦介入率
  `0.07%-0.22%`，远低于 `23.7Nm` 上限。问题不是电机力矩不足。
- MuJoCo 前进时右/左脚净间隙 p50 为 `0.68/0.01cm`；后退速度几乎为 0、双脚
  接触率接近 100%。sim2sim 放大了训练策略已有的低离地裕量和后退退化。
- 参考库左右接触率均为 `0.60417`，镜像关节幅度最大差小于 `1.3e-4rad`，排除参考
  轨迹左右不对称。TensorBoard 的左右接触率差异来自学习后的策略。
- 零头命令时 IsaacLab `n3/n4` 仍约 `0.04-0.05rad RMS`；当前 neck 权重只有论文
  Table I 的一部分，是无命令扭头未被充分约束的直接原因。

## 保持论文架构的参数修正

- 不增加新奖励项，继续使用论文的参考运动模仿、接触模仿、动作正则和存活奖励结构。
- 参考摆动脚峰值从 `3.5cm` 提高到 `5cm`，给 sim2sim/真机跟踪误差保留约
  `1.5cm` 裕量；重新生成 `reference_gait.npz`，最差 IK 位置误差 `2.04mm`。
- 接触 mismatch 权重保持在 `3.0`，不再从启动值退火到 `1.5`，防止后期策略用
  持续双支撑换取躯干和速度跟踪收益。
- 默认周期头部起伏关节幅度从 `0.015rad` 降到 `0.005rad`，约为原来的 1/3；
  `n3/n4` 参考继续严格为 0。
- neck 奖励恢复论文 Table I：位置 `-100`、速度 `-1`、action rate `-5`、
  action acceleration `-5`。
- TensorBoard 新增前进/后退分方向的左右接触率和参考摆动相实际离地间隙，避免
  全命令均值掩盖单脚或后退退化。
- 扩展 `scripts/diag_walk_neck.py`，输出逐脚摆动率/间隙、左右腿追踪 RMS、力矩
  p99/限幅介入率以及脖子关节和目标 RMS。

## 验证

- 新参考库：`foot_clearance=0.05m`，neck 峰值 `[0.005,0.005,0,0]rad`；零命令
  `phase_rate=0`、neck=0、双脚支撑；左右轨迹保持镜像对称。
- 16-env 开环参考回放平均存活 `0.67s`，与旧参考约 `0.7s` 同级，提高抬脚没有
  明显降低开环动力学可行性。
- 64-env、1-iteration PPO smoke test 通过，actor/critic/action 仍为 `80/82/14`，
  新指标正常写入，无 traceback/NaN。

## model_22100 持续摔倒诊断与修正

- `2026-07-11_22-55-52` 到 `22244 iter` 的 `fall_rate` 始终约为 `100%`，
  `time_out_rate=0`，平均 episode 只有约 `51 steps/1.02s`，不是偶发摔倒。
- 关闭头命令、扰动、噪声和延迟后，前进命令 `+0.15m/s` 实际达到
  `+0.363m/s`；后退命令 `-0.15m/s` 实际仍向前 `+0.215m/s`，且机身
  roll/pitch 角速度 p95 约 `3.8-4.0rad/s`。基础腿策略已经过度迈步并失稳。
- 腿部力矩 p99 仅约 `4.7-5.5Nm`，执行器模型介入率约 `2%-3.5%`，排除电机
  `23.7Nm` 力矩上限不足是 IsaacLab 摔倒主因。
- 接触 mismatch 权重从 `3.0` 降到 `2.0`，在保留论文接触模仿结构的同时允许
  策略偏离开环参考并进行平衡修正；存活权重从 `2.0` 提高到 `5.0`。
- 头命令课程与动作平滑课程解耦：前约 `1000 iter` 保持零头命令，随后约
  `2000 iter` 连续放开到完整论文命令范围。
- 论文扰动前约 `1000 iter` 保持关闭，随后约 `2000 iter` 线性增加到完整强度；
  rough runner 按每 iteration 的 control-step 数等比例设置。
- checkpoint 续训时统一恢复奖励、头命令和扰动课程进度，避免 resume 后头命令与
  扰动强度意外从零重新开始。
- TensorBoard 新增前进/后退各自的实际 `vx`、命令 `vx` 和 `vx MAE`，直接暴露
  方向反转问题，不再只依赖全方向平均的 `velocity_mae`。
- 同步修正环境模块中遗留的“固定脖子、57/59 维”旧架构说明；当前实现为
  14 个学习动作、80 维 actor 观测和 82 维 critic 状态。

## 本轮验证

- `git diff --check` 与相关 Python 文件语法检查通过。
- 64-env、1-iteration PPO smoke test 通过；日志确认初始课程为
  `contact_match=2.0`、`head_command_scale=0`、`disturbance_scale=0`，新增方向速度
  指标正常写入且无 traceback/NaN。
