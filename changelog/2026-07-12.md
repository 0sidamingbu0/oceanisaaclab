# 2026-07-12

## 18-06-48 训练诊断

- 完整 TensorBoard 日志到 `3180 iter`。策略在 `1300-1600 iter` 已经学稳；
  `model_1400` 附近 `fall_rate≈1.9%`、平均 episode `≈394/400 steps`、
  value loss `≈0.16`，不是基础步态训练失败。
- 头命令与论文扰动从 `1000 iter` 同时开始增强。到 `3000 iter` 两者接近满强度时，
  `fall_rate≈52%`、value loss `≈32`，neck 位置奖励从约 `-0.15` 降到 `-8.96`；
  性能恶化与头命令幅度高度相关。
- `model_3100` 在零头命令、关闭扰动/噪声/延迟后仍能以 `0.152m/s` 跟踪
  `0.15m/s`，roll/pitch 角速度 p95 `1.16rad/s`。说明训练分布过强，但腿策略尚未
  被彻底破坏。
- 原方向速度 TensorBoard 指标遗漏了 `forward_vx_sign`，导致前进显示为负值；
  这是诊断指标符号错误，不是策略把前后方向学反。

## 参数修正

- 保持论文的 4-DOF 头命令接口、映射和 neck 奖励权重，将本机训练命令范围适配为
  原范围约 1/3：头高 `±0.007m`、pitch `±0.17rad`、yaw `±0.33rad`、
  roll `±0.20rad`。
- 头目标重采样间隔从 `1.5-3.5s` 延长到 `4-8s`，匹配脖子执行器 `1rad/s`
  速度限制，减少四轴大幅阶跃产生的持续跟踪误差。
- 头命令课程仍在约 `1000-3000 iter` 渐进完成；论文扰动改为在约 `3000 iter`
  后启动，并用约 `3000 iter` 渐进到完整强度，避免两个困难课程同时增强。
- walking PPO 的 entropy 系数从论文静态任务配置 `0` 适配为 `0.001`；当前策略在
  课程完成前 std 已降到约 `0.05`，小幅探索正则用于保留适应后续分布变化的能力。
  perpetual standing runner 仍保持 `entropy_coef=0`。
- 修正前进/后退实际速度和 MAE 的物理前向符号；新增
  `Metrics/neck_tracking_rms_rad`，直接监控脖子目标跟踪误差。

## 续训建议

- 不继续当前 `model_3100` 的优化器状态。建议从性能峰值附近的
  `logs/rsl_rl/bdx_walk_imitation/2026-07-12_18-06-48/model_1400.pt` 恢复到新 run，
  保留已学会的稳定腿步态，再学习缩小后的头命令与后置扰动课程。

## 验证

- 相关 Python 文件 AST 语法检查与 `git diff --check` 通过。
- 64-env、1-iteration 从头训练 smoke test 通过，修正后的方向速度指标和
  `Metrics/neck_tracking_rms_rad` 正常写入。
- 64-env 从 `model_1400.pt` 恢复的 1-iteration smoke test 通过：课程恢复到
  `head_command_scale=0.2002`、`disturbance_scale=0`，该批次 `fall_rate=0`、
  neck tracking RMS `0.0184rad`，无 traceback/NaN。
