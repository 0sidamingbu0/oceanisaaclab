# 2026-07-12

## 18-06-48 训练诊断

- 完整 TensorBoard 日志到 `3180 iter`。策略在 `1300-1600 iter` 已经学稳；
  `model_1400` 附近 `fall_rate≈1.9%`、平均 episode `≈394/400 steps`、
  value loss `≈0.16`，不是基础步态训练失败。
- 头命令与论文扰动从 `1000 iter` 同时开始增强。到 `3000 iter` 两者接近满强度时，
  `fall_rate≈52%`、value loss `≈32`，neck 位置奖励从约 `-0.15` 降到 `-8.96`；
  性能恶化与头命令幅度高度相关。
- `model_3100` 在零头命令、关闭扰动/噪声/延迟后仍能以 `0.152m/s` 跟踪
  `0.15m/s`，roll/pitch 角速度 p95 `1.16rad/s`。说明训练分布过强，但腿策略尚未
  被彻底破坏。
- 原方向速度 TensorBoard 指标遗漏了 `forward_vx_sign`，导致前进显示为负值；
  这是诊断指标符号错误，不是策略把前后方向学反。

## 参数修正

- 保持论文的 4-DOF 头命令接口、映射和 neck 奖励权重，将本机训练命令范围适配为
  原范围约 1/3：头高 `±0.007m`、pitch `±0.17rad`、yaw `±0.33rad`、
  roll `±0.20rad`。
- 头目标重采样间隔从 `1.5-3.5s` 延长到 `4-8s`，匹配脖子执行器 `1rad/s`
  速度限制，减少四轴大幅阶跃产生的持续跟踪误差。
- 头命令课程仍在约 `1000-3000 iter` 渐进完成；论文扰动改为在约 `3000 iter`
  后启动，并用约 `3000 iter` 渐进到完整强度，避免两个困难课程同时增强。
- walking PPO 的 entropy 系数从论文静态任务配置 `0` 适配为 `0.001`；当前策略在
  课程完成前 std 已降到约 `0.05`，小幅探索正则用于保留适应后续分布变化的能力。
  perpetual standing runner 仍保持 `entropy_coef=0`。
- 修正前进/后退实际速度和 MAE 的物理前向符号；新增
  `Metrics/neck_tracking_rms_rad`，直接监控脖子目标跟踪误差。

## 续训建议

- 不继续当前 `model_3100` 的优化器状态。建议从性能峰值附近的
  `logs/rsl_rl/bdx_walk_imitation/2026-07-12_18-06-48/model_1400.pt` 恢复到新 run，
  保留已学会的稳定腿步态，再学习缩小后的头命令与后置扰动课程。

## 验证

- 相关 Python 文件 AST 语法检查与 `git diff --check` 通过。
- 64-env、1-iteration 从头训练 smoke test 通过，修正后的方向速度指标和
  `Metrics/neck_tracking_rms_rad` 正常写入。
- 64-env 从 `model_1400.pt` 恢复的 1-iteration smoke test 通过：课程恢复到
  `head_command_scale=0.2002`、`disturbance_scale=0`，该批次 `fall_rate=0`、
  neck tracking RMS `0.0184rad`，无 traceback/NaN。

## StandPaper 与策略切换重构

- 站立 policy/critic 从旧 `74/76` 改为 `77/79` 维，补入 `projected_gravity`，动作仍为
  10 腿 + 4 脖子。旧站立 checkpoint/ONNX 不兼容，必须从头训练和重新导出。
- `stand_pose.npz` 的精确零命令、行走参考零速帧和 URDF q=0 现在共享同一腿角、base height、
  双脚锚点和脚 frame yaw 标定；公共 `base_pos_pf=[0.04638837,-0.00091510]m`，不再把真实
  `4.64cm` 前向偏移错误归零。生成器高度网格显式包含 0，并保存 path-frame 躯干参考。
- 站立 torso 可行命令域收窄为 h `[-0.04,+0.01]m`、pitch `±0.17rad`、yaw `±0.24rad`、
  roll `±0.09rad`；torso/head 命令在 episode 内每 `4-8s` 重采样。
- 站立零命令比例提高到 50%；启用独立 stand RSI，禁用父类 walking RSI。reset 时物理 q、
  14维动作延迟环、前两帧动作、腿/脖子 FOH 与低通目标全部同步到同一可表示 setpoint，
  修复启动时重放随机行走动作造成的快速抖动。
- path frame 站立分支改为收敛到双脚中心与校准后的双脚平均 heading，而非躯干 yaw；
  站立 yaw 奖励跟踪离线 solved reference 的 `base_yaw_pf`。
- 站立奖励恢复论文 Table I：torso xy 核 200、orientation 权重 1、survival 20，移除额外
  torso-height reward；高度命令继续通过腿关节参考表达，torque/joint-acc 项覆盖腿和脖子全部
  14 个关节。扰动按论文在前 1500 iter 线性放开。
- 腿和脖子 14维目标统一经过 50Hz→200Hz FOH + 37.5Hz 低通。该 plant 变化也影响 walking；
  现有 80维 walking 模型至少需要完整回归，推荐重新训练或微调。

## StandPaper 验证与边界

- Python AST/compileall、`git diff --check` 通过；64 env、1 PPO iteration smoke test 通过，
  actor/critic/action=`77/79/14`，`fall_rate=0`，无 traceback/NaN。
- 范围内非零 yaw RSI 的 reference 重构误差约 `2.7e-7rad`，solved 与 Isaac 双脚 heading
  差约 `0.00176rad`；reset 后腿/脖子 target-q、动作环和前两帧动作误差均为 0。
- stand/walk q=0 脚距均为 `0.25169745297m`，差约 `2.1e-12m`；base height 均为
  `0.38498640060424805m`，`base_pos_pf` 完全一致。
- 新可行域完整 625 点网格的 stand IK 最大脚残差为 `2.899mm`；最大原始归一化动作约
  `0.7364`，6250 个腿关节标量中 action-range/soft-limit 夹持数量均为 0。

## Python sim2sim 最终对齐

- path-frame FK 改用末端脚 link body origin/quaternion，对齐 IsaacLab 的 body state；此前 sole
  geom center 会制造约 `2.585cm` 的 neutral 位置偏差。sole geom 仅继续用于脚-地接触判定。
- walk→stand 在下一双支撑切换时，以实测躯干高度和姿态初始化 standing torso 命令；切换保留
  两帧动作、当前腿/脖子 setpoint、FOH 和低通状态。
- `STAND_UP→RL_STAND` 先以实测躯干姿态初始化命令，再用 `0.5s` 余弦接管平滑回
  用户/neutral 命令，同时渐入腿/脖子策略目标并把起立脚本 `50/3` 切到论文 Go1 低层
  `10/0.3`，避免新目标在高增益阶段造成启动力矩突变。
- 真实 MuJoCo 使用现有 80维 walking policy 作为 standing fallback 回归：首个 RL 控制步
  applied 腿目标跳变从修改前 `0.09057rad` 降到 `2.24e-5rad`，腿力矩跳变从 `4.591Nm`
  降到 `0.079Nm`；完整 `0.5s` 接管窗最大单步目标/力矩变化为 `0.00681rad/0.175Nm`，
  脖子最大单步目标/力矩变化为 `0.000301rad/0.0495Nm`。该 fallback 测试只验证状态机和
  低层连续性，不能替代重训后的 77维 standing policy 验收。
