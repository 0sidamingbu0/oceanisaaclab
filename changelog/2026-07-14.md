# 2026-07-14

## StandPaper 表现范围与主动恢复

- 复查既有站立 run `2026-07-12_23-56-34`：约 14838 iteration 时 `contact_match≈1.961/2`、
  `fall_rate≈1.46%`、episode length `≈395/400`，但 policy std 已接近旧下限 `0.03`。策略主要
  收敛到静态双脚支撑，侧推时只有短暂约 `12mm` 离地，没有形成明确恢复步；继续沿用旧奖励和
  checkpoint 不足以解决主动迈步。
- StandPaper head 命令恢复 `neck_head_map.npz` 的完整域：Δh `±0.02m`、pitch `±0.50rad`、
  yaw `±1.00rad`、roll `±0.60rad`。walking 仍保持约 1/3 域
  (`±0.007m / ±0.17 / ±0.33 / ±0.20rad`)，避免重新引入行进甩头。
- 保持论文 Table I 奖励集合和静态双脚参考，不新增脚高、指定左右脚或迈步奖励。新增连续受扰
  门控：由相对命令姿态的 roll/pitch 倾斜误差、body 水平速度、body roll/pitch 角速度和
  path-frame torso 平面位置误差四路信号触发，只在失衡时放松现有双脚接触、腿姿态模仿、腿
  action rate/action acceleration 约束。躯干位姿、线/角速度、survival、力矩和关节加速度目标
  始终保留，让策略自行选择恢复步。
- 门控从 tilt `0.08→0.22rad`、水平速度 `0.18→0.45m/s`、roll/pitch 角速度
  `0.45→1.40rad/s`、平面偏移 `0.025→0.065m` 线性增强。命令重采样后 `0.35s` 内只屏蔽
  瞬时 tilt error，真实速度、角速度和位置误差仍可触发。stand_pose 全命令域的 `base_pos_pf`
  相对 neutral 最大只变 `1.70mm`，远低于 `25mm` position gate 起点；保留位置通道不会误判
  命令跳变，并消除了每次重采样后的慢速平移失稳盲区。
- raw 门控快速开启，单脚支撑期间持续保持；恢复双脚支撑后 hold `0.30s`，再按满量程
  `0.40s` 线性慢释放。其对奖励权重的作用与论文扰动同步在前 1500 iteration / 36000 steps
  从 0 放到 1，防止随机初始策略立即关闭稳站约束。课程放满且满门控时 contact 缩放为 0，
  腿姿态权重 `-15→-3`、腿 action rate `-1.5→-0.25`、腿 action acceleration
  `-0.45→-0.075`；稳定时恢复 Table I 权重。
- Stand PPO 改为 `entropy_coef=0.001`、`min_std=0.05`，避免大扰动课程放开前探索方差已塌缩。

## 诊断与迁移要求

- 修正站立环境原先恒为零的左右脚接触率、双支撑率和 neck tracking 指标；新增 raw
  `Metrics/recovery_signal_mean`、有效 `Metrics/recovery_gate_mean`、
  `Curriculum/recovery_gate_scale`、`Metrics/recovery_active_rate`、
  `Metrics/recovery_single_support_rate`、`Metrics/liftoff_events_per_episode` 和
  `Metrics/recovery_liftoff_events_per_episode`，用于区分“门控被触发”“单脚卸载”和“恢复期
  实际离地”。同时记录全 episode/恢复期的 touchdown 事件和 liftoff→touchdown 平均脚 XY
  落脚距离（cm），用于区分原地卸载与实际捕获步。
- 当前命令分布、奖励目标和探索分布均已改变。旧 `74/76` 模型形状不兼容；此前的 77 维 stand
  checkpoint/ONNX 虽可加载，但只见过缩小 head 域，不能 resume 或直接接收完整命令。必须从头
  训练 StandPaper 并重新导出，不能续训 `model_14800.pt`。
- OceanBDX Python sim2sim 同步 stand/walk 独立 head 限幅；新的完整站立范围只能配套重新训练
  后的 StandPaper ONNX。C++ 真机主控仍是旧单策略接口，本次没有扩大其部署能力。
- 最新完整配置通过 64 env × 1 PPO iteration smoke test：`77/79/14`、`fall_rate=0`，无
  traceback/NaN。首轮 `Metrics/recovery_signal_mean=0.9910`，但
  `Curriculum/recovery_gate_scale=0.0003`、有效 `Metrics/recovery_gate_mean=0.0002`，证明早期
  门控课程确实保留了稳站约束。完整从头训练和侧推恢复效果仍需后续 checkpoint 验收。
- 更新根 README 与 `source/oceanisaaclab/docs/BDX_STAND_TRAINING.md`，记录四路恢复信号、门控
  状态机/课程、PPO 参数、新诊断指标和模型迁移边界。
