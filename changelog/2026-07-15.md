# 2026-07-15

## 站立策略恢复论文固定奖励

- 审计 run `logs/rsl_rl/bdx_stand_perpetual/2026-07-14_19-56-35`（约 21.4k iter）：
  `recovery_active_rate≈99.9%`、`recovery_signal≈0.97`、每 episode 离地约 `7.5` 次，
  平均离地时间约 `0.07s`、落脚 XY 位移约 `2.7cm`，同时 `double_support≈0.943`、
  `velocity_mae` 持续变差。该行为是接触闪断/小碎挪，不是大扰动捕获步；继续训练只会固化该局部解。
- 根因与论文警告一致：旧 recovery gate 长期把 contact 权重近似降为 0、腿姿态 `-15→-3`、
  腿 action rate/acc 放松约 83%，策略因此学会 rapid foot shuffling。训练扰动在旧配置中也几乎
  没有持续 quiet window，进一步放大了这个正反馈。
- 论文 Appendix A 明确所有 policy 使用 Table I 奖励，Robustness 允许策略在大推时偏离 reference
  trajectory/contact schedule；因此删除 reward-dependent recovery gate，不新增脚高、指定脚或
  stepping reward。站立环境现在所有状态固定使用 Table I contact、leg imitation 和 smoothness。
- 保留 recovery 信号仅作真机可用诊断：Schmitt 状态在归一化失稳信号 `0.35` 进入，低于 `0.10` 且
  双脚稳定 `0.30s` 退出；不修改奖励、不修改动作。新增 `recovery_capture_steps_per_episode`、
  `quiet_liftoff_events_per_episode`、`quiet_foot_drift_cm`，区分有效捕获步和稳态脚抖。
- Stand PPO 恢复论文 Table IV：`entropy_coef=0`，移除站立专用 `min_std=0.05` 覆盖，回到默认
  `min_std=0.03`。旧 21k checkpoint/ONNX 不可续训，必须从头训练 100k iter。
- 训练验收标准：无扰动窗口 `quiet_liftoff` 接近 0、双支撑稳定；大推后才出现满足至少 `0.12s`
  离地和 `3cm` 落脚位移的捕获步，并在落地后恢复双支撑；同时 fall rate 下降。
- 站立参考已改为 head4+torso4 联合全身 CoM 平衡。为避免 `5^8` 表，资产使用 factorized
  表示：torso 静态平衡节点 + head 全身 CoM 偏移 + 腿 IK/基座平移局部雅可比；训练时仍只做
  两次 4D 插值。脚 link 原点中点不再被误当成支撑中心，生成器用 collision sole 低层几何校验
  neutral CoM，并保留精确 q=0 作为 stand/walk 公共切换姿态。
- 新资产验证：625 个 torso 节点最大 neutral-head CoM/脚误差 `0.020mm / 0.886mm`；叠加
  16 个 head 极限角点后最大 CoM/脚误差 `0.080mm / 4.220mm`。`--viz-only` 现在播放保存的
  联合参考并扫掠 torso4/head4，而不是重新求一个仅 torso 的 IK。

## 验证

- `python3 -m py_compile`：通过（stand env/config、PPO config、diagnostic script）。
- `git diff --check`：通过。
- 固定 Table I 和 clean/disturbed 分层后的 64-env×1 PPO smoke test 已完成：
  actor `77→14`、critic `79→1`，fall rate `0`，无 NaN/traceback；nested contact-view warning
  仍是既有初始化噪声。

## 训练分布修正

- 旧 Table V 进程中 4 个短小扰动和 2 个长小扰动独立运行，所有 body 同时关闭的 quiet 窗口过少，
  造成站立策略在无大推时也持续小幅卸载/挪脚。新增 `stand_disturbance_quiet_prob=0.50`：
  每个 episode 以 50% 概率关闭全部外力作为 nominal 锁脚基线，其余 episode 保留完整 Table V
  扰动和课程；不改变奖励、不新增抬脚奖励、不向 policy 暴露该标签。
- 新增 `clean_episode_fraction`、`clean_liftoff_events_per_episode`、
  `clean_double_support_rate`，分别验收无外力样本比例、无扰离地和双支撑稳定性。
- 分层配置的 64-env×1 iteration smoke test 已通过；旧 21k gate checkpoint 仍不可续训，正式训练
  必须从头开始。
