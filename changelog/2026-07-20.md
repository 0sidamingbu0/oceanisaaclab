# 2026-07-20

## Walking 头部命令范围

- walking 的头部命令由完整可达域约 `1/3` 提高到严格 `1/2`：头高 `±0.01m`、pitch
  `±0.25rad`、yaw `±0.50rad`、roll `±0.30rad`。
- standing 继续使用此前缩小后的 `2/3` 范围，不受本次修改影响。
- OceanBDX 部署限幅同步更新。现有 walking ONNX 按旧 `1/3` 范围训练，需要用新范围训练或
  微调并重新导出，才能可靠覆盖扩大的命令域。
